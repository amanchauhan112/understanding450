import React from "react";
import "../../Answers/answer.css";

const sq = () => {
  return (
    <div className="div">

      <div className="ans_heading">
        <h1>LRU Cache</h1>
      </div>

<br/>
      <div className="solution">
        <h2>Solution will be added soon....</h2>
        {/* <p>Before going to approach let's understand LRU Cache:</p>
        <p>We need to create a data structure of size = cap and we will have two functions:</p>
        <p><strong>set(key,value):</strong> It will set position key with value</p>
        <p><strong>get(key):</strong> It prints value at position : key</p>

      <h1>Optimal Approach :</h1>
<p>Now let's see approach : </p>
<p>One approach can be : use array of size cap and do required operations</p>
<p>This will take O(n) Time</p>

        <h2>Code</h2>

        <h3>
          T.C:O(n)
          <br />
          S.C:O(1)
        </h3>

        <pre className="code">
          {`
   
    
            `}
        </pre> */}
      </div>
    </div>
  );
};

export default sq;
